<% if !@order_item.nil? && @order_item.errors.any? %>
    <div class="alert alert-danger">
      <ul>
        <% @order_item.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>

<% if @order_items.nil? or @order_items.size == 0 %>
    <p class="text-center mt2">
      В вашей корзине нет ни одного товара. Пожалуйста <%= link_to 'перейдите назад', root_path %> и добавьте товары в корзину.
    </p>
<% else %>
    <div class="p1 glassy-bg mb1 text-center radius-l1 radius-r1">
      <h4>Моя корзина</h4>
      <p class="mb0"> Товаров в корзине <span class="cart-count"><%= @order_items.size %></span></p>
    </div>

    <% @order_items.each do |order_item| %>
        <%= render 'carts/cart_row', product: order_item.product, order_item: order_item, show_total: true %>
    <% end %>


    <h4 class="mb1"> Итого к оплате:
      <span style="color: green"><%= number_to_currency(current_order.subtotal, locale: :ru) %></span>
    </h4>

    <div class="input-group-btn">
      <%= link_to 'Оформить заказ', new_payment_path, class: 'button' %>
    </div>


<% end %>