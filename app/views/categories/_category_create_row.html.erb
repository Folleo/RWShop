<%= form_for(@category, html: {class: 'new_form', method: :post}, remote: true) do |f| %>
    <div class="row">
      <div class="small-2 columns"><%= f.text_field :name %></div>
      <div class="small-2 columns"><%= f.text_field :description %></div>
      <div class="small-2 columns"><%= f.text_field :discount_percent %></div>
      <div class="small-2 columns"><%= f.text_field :discount_amount %></div>
      <div class="small-3 columns"><%= f.text_field :image %></div>
      <div class="small-1 column end">
        <%= link_to categories_path, :onclick => "$('.new_form').submit()", remote: true do %>
            <i class="fi-save"></i>
        <% end %>&nbsp;&nbsp;&nbsp;&nbsp;
        <%= link_to 'Cancel', remote: true do %>
            <i class="fi-minus-circle"></i>
        <% end %>
      </div>
    </div>
<% end %>

